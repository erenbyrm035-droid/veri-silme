<?xml version="1.0" encoding="utf-8"?>
<!--
    Viva AI Coach — TWA kabuğu manifesti.

    "No Activity" şablonunun ürettiği app/src/main/AndroidManifest.xml
    dosyasının YERİNE bunu koy.

    Burada kendi Activity'miz YOK: uygulamayı açan LauncherActivity
    androidbrowserhelper kütüphanesinden geliyor. Yazdığımız tek Kotlin
    dosyası DelegationService — o da yalnızca Digital Goods API'yi açıyor.

    Meta-data anahtarlarının tamamı kütüphanenin LauncherActivityMetadata.java
    dosyasından birebir alındı; harfi harfine yazılmalı, yoksa sessizce
    yok sayılırlar.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Play Billing (Digital Goods API). Premium satışı kapalıysa sil. -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
            Digital Asset Links: uygulamanın hangi siteye ait olduğunu Chrome'a
            bildirir. Sitedeki karşılığı public/.well-known/assetlinks.json.
            İKİSİ de doğru olmadan adres çubuğu gizlenmez.
        -->
        <meta-data
            android:name="asset_statements"
            android:resource="@string/assetStatements" />

        <!-- ChromeOS'ta PWA olarak tanınması için -->
        <meta-data
            android:name="web_manifest_url"
            android:value="@string/webManifestUrl" />

        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:label="@string/app_name"
            android:alwaysRetainTaskState="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="true">

            <!-- Açılışta gidilecek adres — manifest.ts'teki start_url ile aynı -->
            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="https://veri-silme.vercel.app/dashboard" />

            <!-- Sistem çubukları: manifest.ts'teki theme_color (#0a0a0b) -->
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/colorPrimary" />
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR_DARK"
                android:resource="@color/colorPrimary" />
            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/backgroundColor" />
            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR_DARK"
                android:resource="@color/backgroundColor" />

            <!-- Açılış ekranı: sayfa yüklenene kadar logo, beyaz flaş olmaz -->
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_IMAGE_DRAWABLE"
                android:resource="@drawable/splash" />
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
                android:resource="@color/backgroundColor" />
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_FADE_OUT_DURATION"
                android:value="300" />
            <meta-data
                android:name="android.support.customtabs.trusted.FILE_PROVIDER_AUTHORITY"
                android:value="com.viva.aicoach.fileprovider" />

            <!-- manifest.ts → orientation: "portrait" -->
            <meta-data
                android:name="android.support.customtabs.trusted.SCREEN_ORIENTATION"
                android:value="portrait" />

            <!-- Chrome yoksa Custom Tab'a düş -->
            <meta-data
                android:name="android.support.customtabs.trusted.FALLBACK_STRATEGY"
                android:value="customtabs" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Deep link: siteye ait bağlantılar uygulamada açılsın.
                autoVerify, assetlinks.json doğrulandıktan SONRA çalışır.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="veri-silme.vercel.app" />
            </intent-filter>
        </activity>

        <activity android:name="com.google.androidbrowserhelper.trusted.FocusActivity" />

        <!-- Splash görselini tarayıcıyla paylaşmak için gerekli -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.viva.aicoach.fileprovider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

        <!--
            Digital Goods API köprüsü. Web tarafı (src/lib/billing/play-client.ts)
            getDigitalGoodsService("https://play.google.com/billing") çağırınca
            istek buraya gelir.
        -->
        <service
            android:name=".VivaDelegationService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.support.customtabs.trusted.TRUSTED_WEB_ACTIVITY_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <activity
            android:name="com.google.androidbrowserhelper.playbilling.provider.PaymentActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:configChanges="keyboardHidden|keyboard|orientation|screenLayout|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="org.chromium.intent.action.PAY" />
            </intent-filter>
            <meta-data
                android:name="org.chromium.default_payment_method_name"
                android:value="https://play.google.com/billing" />
        </activity>

        <service
            android:name="com.google.androidbrowserhelper.playbilling.provider.PaymentService"
            android:exported="true">
            <intent-filter>
                <action android:name="org.chromium.intent.action.IS_READY_TO_PAY" />
            </intent-filter>
        </service>

    </application>

</manifest>
